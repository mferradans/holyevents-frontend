import React from 'react';
import './Spinner.css';  // Asegúrate de crear este archivo CSS

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="loading-spinner">
      </div>
    </div>
  );
};

export default Spinner;
